---
applications:
- name: design-pattern-app
  memory: 1024M
  disk_quota: 1024M
  instances: 1
  buildpacks:
    - nodejs_buildpack
  stack: cflinuxfs4
  command: npm run build && npm start
  env:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false
  services: [] 